# ArtSense

## Requirements
- Docker;
- Node (npm, npx);

## How to run

### Initialize the backend
```bash
docker compose up --build -d
```

### Run the expo app
```bash
cd frontend
npm install
npx expo start
```

### Scan the QRCode

